var cherrio = require('cherrio'),
    fs      = require('fs');

    function GetHtml(argument) {
      this.directory
      }

    function getHtml(file) {
      
    }

    function getFiles(argument) {
      // body...
    }
